<div class="sample-hero">
  <div class="sample-hero_inner">
    <h2 class="sample-hero_title">Hero Block</h2>
    <p class="sample-hero_lead">This is a simple website sample.</p>
  </div>
</div>

<style lang="scss">
  @use '@/styles/_develop/+' as *;

  .sample-hero {
    display: grid;
    place-items: center;
    min-height: rem(480);
    padding-inline: var(--space-inline);
    margin-block-start: 3lh;
    background-image: linear-gradient(120deg, rgb(255 84 54 / 65%), rgb(255 255 255 / 0%)),
      linear-gradient(185deg, rgb(69 54 255 / 65%), rgb(255 255 255 / 0%)),
      linear-gradient(340deg, rgb(0 153 26 / 65%), rgb(255 255 255 / 0%));
  }

  .sample-hero_inner {
    padding-block: var(--space-block);
    color: var(--color-text);
    text-align: center;
  }

  .sample-hero_title {
    font-size: var(--typo-size-900);
  }
</style>
